extends layout
block content
  .container
    .col-md-12
      h2 STOCK ENTRY
      div(ng-app='myApp')
        div( ng-controller='ExampleController')
          form(name='myForm')
            label(for='singleSelect') Vendor: 
            br
            select(id = 'vendor', name='singleSelect', ng-model='data.singleSelect' , ng-change='updateItems()')
              each val,index in vname
                option(value='option-'+index) #{val.vname}
            br
            label(for='singleSelect2') Items: 
            br
            .allItems
              select(id ='item',name='singleSelect2', ng-model='data.singleSelect2')
                each val,index in iname
                  option(value='option-'+index) #{val.iname}
            br
        div(ng-controller='DateController')
          form
            .form-group
              label.control-label(for='request-date')  Date:
              .control
                input#request-date.form-control(type='date', ng-model='requestDate')
        div( ng-controller='myCtrl')
          form(name='myForm')
              label
                h4 Stores:
              br
              .checkbox(ng-repeat='product in products')
                input(type='checkbox', ng-model='product.selected', ng-change='add(product)')
                |  {{product.name}}
              br
              button( ng-click= 'selectAll()' )
                | Select All
              button( ng-click= 'deselectAll()' ) 
                | Deselect All
              br
              br
              button(style='margin-bottom:30px', ng-click='saveAll()')
                | Enter 
